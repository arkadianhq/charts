apiVersion: v1
kind: Service
metadata:
  name: {{ include "spock.fullname" . }}
  labels:
    {{- include "spock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spock.selectorLabels" . | nindent 4 }}
