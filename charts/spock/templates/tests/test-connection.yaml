apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spock.fullname" . }}-test-connection"
  labels:
    {{- include "spock.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spock.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
